services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: openfoodfacts-kmeans-app
    network_mode: host
    volumes:
      - ./data:/app/data
    depends_on:
      datamart:
        condition: service_healthy

  datamart:
    build:
      context: .
      dockerfile: Dockerfile.datamart
    container_name: openfoodfacts-datamart
    network_mode: host
    environment:
      MSSQL_HOST: localhost
      MSSQL_PASSWORD: ${MSSQL_PASSWORD:-QwErTy123@!}
    ports:
      - "8090:8090"
      - "4040:4040"
    volumes:
      - ./data:/app/data
    healthcheck:
      test: ["CMD-SHELL", "bash -c '</dev/tcp/localhost/8090'"]
      interval: 5s
      timeout: 3s
      retries: 30
      start_period: 60s
    depends_on:
      mssql:
        condition: service_healthy

  mssql:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: openfoodfacts-mssql
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: ${MSSQL_PASSWORD:-QwErTy123@!}
    ports:
      - "1433:1433"
    volumes:
      - mssql_data:/var/opt/mssql
    healthcheck:
      test: ["CMD-SHELL", "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P \"$$MSSQL_SA_PASSWORD\" -No -Q 'SELECT 1' || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

volumes:
  mssql_data:
